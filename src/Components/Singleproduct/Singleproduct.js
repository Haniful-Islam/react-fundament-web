import React from 'react';

const Singleproduct = (props) => {
    console.log(props)
    return (
        <div>
            <h2>Single Products</h2>
        </div>
    );
};

export default Singleproduct;